#!/usr/bin/env bash
set -euo pipefail

export DISPLAY="${DISPLAY:-:1}"
export HOME="${HOME:-/home/vm}"
export XDG_RUNTIME_DIR="${XDG_RUNTIME_DIR:-/run/user/$(id -u)}"

until xdpyinfo -display "${DISPLAY}" >/dev/null 2>&1; do
  sleep 0.2
done

exec chrome about:blank
